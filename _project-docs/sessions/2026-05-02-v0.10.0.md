# Session — v0.10.0
**Date:** 2026-05-02
**Version:** 0.9.10 → 0.10.0

## What We Did

Two parallel tracks landed in this session.

**Vercel + web foundation**
- Scaffolded `web/` Next.js 16 (App Router, TypeScript, Tailwind v4) subfolder of the existing repo
- Built `/auth/verified` and `/auth/reset-password` static landing pages with `lumenai://` deep-link redirect
- Wrote Vercel deployment guide; deployed live at `https://lumenai-vert.vercel.app`
- During the push, stripped two oversized FLIR SDK tarballs from git history (`atlas-java-sdk-android-2.19.0.tar.gz` 102 MB, `Android-samples-sources-all.tar.gz` 1.3 MB) via `git filter-branch`, then force-pushed with `--force-with-lease`

**Supabase schema rollout**
- Designed and applied the full new schema as 5 migrations
- Fixed a leftover `trg_on_auth_user_created` trigger from a prior setup that double-fired `handle_new_user`
- Worked around `auth.identities.email` being a generated column in current Supabase
- Seeded the single admin account (`transistor@lumenai.com` / `@dmin123!`)

## Files Created

| File | Purpose |
|---|---|
| `web/` (full Next.js scaffold) | Email verification + reset landing pages, future admin webapp |
| `web/app/auth/layout.tsx` | Shared chrome for auth pages |
| `web/app/auth/verified/page.tsx` + `VerifiedClient.tsx` | Account verification redirect |
| `web/app/auth/reset-password/page.tsx` + `ResetPasswordClient.tsx` | Password reset redirect |
| `web/README.md` | Vercel deployment guide |
| `supabase/migrations/20260502120000_schema_bootstrap.sql` | Enums, PSGC tables, core tables, indexes |
| `supabase/migrations/20260502120100_triggers_and_code_generators.sql` | updated_at, gen_patient_code, gen_clinic_code, handle_new_user |
| `supabase/migrations/20260502120200_rls_policies.sql` | 38 RLS policies + 3 helper functions |
| `supabase/migrations/20260502120300_storage_buckets.sql` | 3 private buckets + 6 storage policies |
| `supabase/migrations/20260502120400_seed_admin_account.sql` | Admin user + identity |
| `_project-docs/sessions/2026-05-02-v0.10.0.md` | This file |

## Files Modified

| File | What changed |
|---|---|
| `CHANGELOG.md` | Added v0.10.0 entry |
| `_project-docs/memory-bank/supabase-changes.md` | Added schema rollout log |
| `_project-docs/memory-bank/activeContext.md` | Added v0.10.0 session summary, demoted v0.9.10 |
| `web/next.config.ts` | Pinned `turbopack.root` to silence workspace-root warning |

## Files Deleted (history rewrite)

- `_project-docs/flir/atlas-java-sdk-android-2.19.0.tar.gz` (was in commits `4383035` + `866ae2b`)
- `_project-docs/flir/Android-samples-sources-all.tar.gz` (same)

The `866ae2b "DELETE: Flir's SDK"` commit was pruned by `--prune-empty`.

## Pending (manual, outside the codebase)

1. Configure Supabase Auth → URL Configuration:
   - Site URL: `https://lumenai-vert.vercel.app`
   - Redirect URL allowlist:
     - `https://lumenai-vert.vercel.app/auth/verified`
     - `https://lumenai-vert.vercel.app/auth/reset-password`
     - `lumenai://auth/account-activated`
     - `lumenai://auth/reset-password`
2. Confirm `https://lumenai-vert.vercel.app/auth/verified` and `/auth/reset-password` render correctly in a desktop browser — done by user.

## Known Issues at End of Session

- Pre-existing TypeScript errors in mobile app (WatermelonDB / Deno / FRAME_HEIGHT) — unchanged from v0.9.10, unrelated to this round.
- Mobile app cannot connect to Supabase yet because the patient signup flow, edge functions, and `app.json` deep-link scheme haven't been wired.
- PSGC/ZIP tables are empty; clinic signup will fail FK checks until seeded.

## Next Session

1. **PSGC + ZIP seed data migration** — load reference data so clinic signup can use the dropdowns.
2. **Edge Functions** — write and deploy `clinic-signup`, `finalize-session`, `promote-session`.
3. **Mobile auth flow rewrite** — `app.json` scheme, deep-link handler in `app/(auth)/*`, role-gate, new signup forms.
4. **Initials avatar component** — 2-letter Teams-style fallback for null `avatar_url`.
