# Session — v0.9.0
**Date:** 2026-04-06
**Version:** 0.8.0 → 0.9.0

## What We Did
- Implemented the full DPN Classification API integration (FR-507)
- Closed all four gaps that prevented the app from delivering a valid JSON payload to the HuggingFace API
- No existing features were broken

## Files Created
| File | Purpose |
|---|---|
| `lib/dpnApi.ts` | Typed API client — `checkServerHealth()` + `scanPatient()`, 60s timeout, mapped HTTP error codes |
| `store/dpnStore.ts` | Zustand slice — `status`, `result`, `error`; `startScan()` health-polls server then runs scan; `clearScan()` |
| `lib/thermal/thermalPng.ts` | Pure-JS PNG encoder — converts thermal `number[][]` matrix to base64 PNG using Iron colormap; no native dependencies |
| `app/(clinic)/dpn-result.tsx` | Full result screen — DPN banner, per-foot cards, asymmetry warning, diagnosis factors, save-to-cloud |
| `_project-docs/sessions/2026-04-06-v0.9.0.md` | This session log |

## Files Modified
| File | What changed |
|---|---|
| `store/sessionStore.ts` | Added `leftMatrix`, `rightMatrix`, `leftImageB64`, `rightImageB64`, `captureLeft()`, `captureRight()`, `clearBilateral()` to `useThermalStore` (additive — no existing fields touched) |
| `app/(clinic)/live-feed.tsx` | Two-step bilateral capture replacing single-frame flow; step indicator UI; image b64 via `expo-file-system` (imported files) or `thermalMatrixToPngB64` (live/CSV); `thermalStore.capture("bilateral")` still called for `clinical-data.tsx` backward compat |
| `app/(clinic)/assessment.tsx` | Removed all mock data and fake progress bar; reads bilateral captures from thermalStore; calls `startScan()` on mount; navigates to `dpn-result` on success; breathing progress animation; error + missing-captures states |
| `app/(clinic)/dpn-result.tsx` | Added save-to-cloud (maps API response to `classification_results` schema, updates session to `completed`); Discard clears session + stores; "Save to Cloud" / "Discard Result" actions |
| `app/(clinic)/_layout.tsx` | Registered `dpn-result` as hidden Tabs.Screen |
| `_project-docs/memory-bank/activeContext.md` | Updated current state, open issues, next steps |
| `_project-docs/memory-bank/progress.md` | Bumped version to 0.9.0, added version history row |
| `CHANGELOG.md` | Added v0.9.0 entry |

## Files Deleted
None

## Pending
- `npx expo run:android` still required (from v0.8.0 — BLE native module)
- `npm install` still needed to remove WatermelonDB from node_modules
- Supabase Edge Function deploy still pending
- UVCModule.kt real AAR still not linked

## Known Issues at End of Session
- `clinical-data.tsx` saves only one thermal frame to Supabase (`capturedMatrix` = last captured foot, "bilateral"); per-foot captures not stored separately — acceptable for v0.9.0
- `asymmetry_mpa_c`, `asymmetry_lpa_c`, `asymmetry_mca_c`, `asymmetry_lca_c`, `angiosomes_flagged`, `bilateral_tci` saved as `null` — the DPN API does not return angiosome-level breakdown
- `model_version` hardcoded as `"dpn-api-v1"` — API does not return a version string
- GAP-08 (angiosome overlay) still deferred — API returns `diagnosis_factors` (strings) not per-angiosome asymmetry values

## Next Session
1. Test the full scan flow end-to-end on device (both feet captured → API call → result displayed → saved to Supabase)
2. Verify PNG encoding is valid (decode b64 on server side and confirm image renders)
3. `npx expo run:android` rebuild for BLE native module
4. GAP-08 — Thermal map angiosome overlay (now unblocked — decide if `diagnosis_factors` strings are enough or if API needs to return angiosome data)
5. UVCModule.kt real implementation — link `libuvccamera-release.aar`
