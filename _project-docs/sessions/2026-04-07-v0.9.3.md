# Session — v0.9.3
**Date:** 2026-04-07
**Version:** 0.9.2 → 0.9.3

## What We Did
- User confirmed `npx expo run:android` native rebuild is complete — BLE + `expo-image-picker` now active
- Cleaned up admin settings to match clinic/patient style (removed Notifications row and version footer)
- Built patient registration form (`register-patient.tsx`) with full field set and Supabase insert
- Added "Add Patient" (`person-add-outline`) button to clinic patient-select header
- Added "Register First Patient" button to patient-select empty state (when no search active)
- Registered `register-patient` as hidden screen in clinic layout

## Files Created
| File | Purpose |
|------|---------|
| `app/(clinic)/register-patient.tsx` | Patient registration: Patient Code, DOB, Sex, Diabetes Type, Duration, Notes → inserts to `patients` → auto-selects → live-feed |

## Files Modified
| File | What changed |
|------|-------------|
| `app/(admin)/settings.tsx` | Removed Notifications row + divider from Account section; removed version footer + `version` style |
| `app/(clinic)/patient-select.tsx` | Added `person-add-outline` header button → register-patient; added "Register First Patient" button to empty state; added `Button` import |
| `app/(clinic)/_layout.tsx` | Registered `register-patient` as hidden `Tabs.Screen` |

## Files Deleted
None.

## Pending
- End-to-end test: bilateral capture → DPN API → save to Supabase (manual, on physical device)
- `npx supabase functions deploy auth-redirect --project-ref yqgpykyogvoawlffkeoq`
- ESP32 firmware BLE + WebSocket configuration

## Known Issues at End of Session
- HW-01: UVCModule.kt is a stub — FLIR Lepton UVC path non-functional until `libuvccamera-release.aar` is linked
- CODE-09: `MOCK_ANGIOSOMES` still used in clinical-data.tsx thermal preview
- CODE-15: duplicate React import in update-password.tsx (cosmetic)
- NAV-01: assessment screen has no back button (intentional by design)

## Next Session
1. End-to-end test: bilateral capture → DPN API → classification result → save to cloud on physical device
2. Link `libuvccamera-release.aar` to enable real FLIR Lepton 3.5 UVC path (HW-01)
3. Offline history screen — review saved captures after offline capture flow
4. Patient role live-feed + history tabs (if needed for thesis demo)
