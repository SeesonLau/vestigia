# Session — v0.9.2
**Date:** 2026-04-07
**Version:** 0.9.1 → 0.9.2

## What We Did
- Added back buttons to 5 screens that had no exit path
- Redesigned clinic settings: removed noise (Notifications, Haptic Feedback, AI Model, version footer), moved Deactivate Account to Profile, wired Privacy Policy / ToS / Contact Support to real screens
- Built clinic profile screen with DB-integrated avatar upload, editable name, account info, Deactivate Account
- Built patient settings redesign matching clinic style
- Built patient profile screen (same structure, "Patient" role badge, no clinic field)
- Created Privacy Policy, Terms of Service, Contact Support screens for both clinic and patient route groups
- Added operator avatar to clinic History screen Cloud tab header
- Set up Supabase Storage `avatars` bucket with 4 RLS policies + `profiles.avatar_url` column
- Installed `expo-image-picker` with dynamic import to avoid native crash pre-rebuild
- Registered all new screens as hidden routes in clinic and patient layouts
- Fixed CODE-18 (unused `label` prop in TabIcon), A11Y-04 (badge contrast), NAV-04 (5 missing back buttons)

## Files Created
| File | Purpose |
|------|---------|
| `app/(clinic)/profile.tsx` | Clinic profile: avatar upload, editable name, account info, deactivate |
| `app/(clinic)/privacy-policy.tsx` | 8-section privacy policy screen |
| `app/(clinic)/terms-of-service.tsx` | 10-section terms of service with clinical disclaimer |
| `app/(clinic)/contact-support.tsx` | Contact channels, FAQ accordion, response times |
| `app/(patient)/profile.tsx` | Patient profile: same structure, "Patient" role |
| `app/(patient)/privacy-policy.tsx` | Patient-route version of privacy policy |
| `app/(patient)/terms-of-service.tsx` | Patient-route version of terms of service |
| `app/(patient)/contact-support.tsx` | Patient-route version of contact support |
| `supabase/migrations/20260407_avatar_support.sql` | avatar_url column + avatars bucket + 4 RLS policies |

## Files Modified
| File | What changed |
|------|-------------|
| `app/(clinic)/settings.tsx` | Full rewrite: removed Notifications, Haptic Feedback, AI Model, version footer, Deactivate Account; Profile/Privacy/ToS/Contact navigate to real screens |
| `app/(clinic)/_layout.tsx` | Registered profile, privacy-policy, terms-of-service, contact-support as hidden screens |
| `app/(clinic)/history.tsx` | Cloud tab header shows operator avatar (Image or initials fallback) |
| `app/(clinic)/patient-select.tsx` | Back button in Header; removed accidental duplicate useTheme import |
| `app/(clinic)/clinical-data.tsx` | Back button in Header; Ionicons import added |
| `app/(clinic)/pairing.tsx` | Back button in Header; useRouter + router declaration added |
| `app/(clinic)/dpn-result.tsx` | Back button on main result view Header |
| `app/(auth)/update-password.tsx` | Floating back arrow in non-done state; TouchableOpacity import added |
| `app/(patient)/settings.tsx` | Full rewrite: proper sections, no flat list, navigates to profile + static screens |
| `app/(patient)/_layout.tsx` | Registered profile, privacy-policy, terms-of-service, contact-support as hidden screens |
| `app.json` | expo-image-picker plugin with camera + photo library permission strings |
| `types/index.ts` | AuthUser: added `avatar_url?: string` |

## Files Deleted
None.

## Pending
- `npx expo run:android` — **required** before expo-image-picker (Change Photo) and react-native-ble-plx (BLE scan) will work
- Run `supabase/migrations/20260407_avatar_support.sql` in Supabase SQL Editor — **done this session by user** ✅

## Known Issues at End of Session
- `expo-image-picker` Change Photo button triggers "Cannot find native module ExponentImagePicker" until `npx expo run:android` is run (dynamic import prevents screen crash but feature non-functional)
- NAV-01: assessment screen has no back button (intentional by design)
- HW-01: UVCModule.kt is a stub — FLIR Lepton UVC path non-functional until libuvccamera-release.aar is linked
- CODE-09: `MOCK_ANGIOSOMES` still used in clinical-data.tsx thermal preview
- CODE-15: duplicate React import in update-password.tsx (cosmetic)

## Next Session
1. Run `npx expo run:android` to unlock BLE + image picker
2. End-to-end test: bilateral capture → DPN API → save to Supabase
3. Admin settings cleanup (match clinic/patient style)
4. Patient registration form in clinic flow (patients must currently be pre-loaded in DB)
5. Add "delete local capture" in History Local tab (`deleteCapture()` already implemented)
