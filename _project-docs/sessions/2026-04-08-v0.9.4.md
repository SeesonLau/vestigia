# Session — v0.9.4
**Date:** 2026-04-08
**Version:** 0.9.3 → 0.9.4

## What We Did

### HW-01 — Real UVC Camera Integration (libuvccamera-release.aar)
- Downloaded saki4510t/UVCCamera zip and updated its build system to compile with modern toolchain:
  - AGP `3.1.4` → `7.4.2`; `jcenter()` → `mavenCentral()`/`google()`; SDK 27 → 33
  - Gradle `8.5` → `7.6.3`
  - Removed `-XX:MaxPermSize=512m` (incompatible with Java 17+)
  - Removed deprecated ABIs (`armeabi`, `mips`); kept `arm64-v8a`, `armeabi-v7a`, `x86_64`
  - Platform `android-14` → `android-21`
- Fixed runtime crash on Android 12+: Added `PendingIntent.FLAG_IMMUTABLE` to `USBMonitor.java`
- Built `libuvccamera-release.aar` successfully; copied into `android/app/libs/`
- Linked into Vestigia: `fileTree(libs)`, `support-v4`, `support-annotations`, `serenegiant:common:2.12.4`
- Added `android.enableJetifier=true` to `android/gradle.properties`
- Added `libcommon` Maven repo to `android/build.gradle`
- Rewrote `UVCModule.kt` from stub to full implementation:
  - `USBMonitor` device lifecycle (attach/connect/disconnect/detach/cancel)
  - Auto-requests USB permission for already-plugged-in devices on `connect()`
  - Opens UVC camera at 160×120 YUYV; `IFrameCallback` encodes raw bytes as Base64 → emits `UVCFrame` event
  - `connect()`, `disconnect()`, `isConnected()` ReactMethods; `cleanup()` helper

### App Name: "Lumenai" → "Lumen AI"
- `app.json` — `"name"` updated
- `constants/strings.ts` — `app.name`, `app.versionFooter`, `auth.loginFooter` all updated
- `android/app/src/main/res/values/strings.xml` — `app_name` updated

### Release APK Build
- `npx expo run:android --variant release` started; build running at end of session

### Commits
1. Large commit — UVC + features (38 files from previous sessions)
2. `UPDATE: rename app display name to "Lumen AI"` — 3 files
- `git push` to remote completed

## Files Created

| File | Purpose |
|------|---------|
| `android/app/libs/libuvccamera-release.aar` | Compiled UVC camera library (fixed build) |

## Files Modified

| File | What changed |
|------|-------------|
| `android/app/src/main/java/com/anonymous/vestigia/UVCModule.kt` | Full implementation — was a stub |
| `android/app/build.gradle` | Added AAR fileTree + support-v4 + support-annotations + serenegiant:common |
| `android/gradle.properties` | Added `android.enableJetifier=true` |
| `android/build.gradle` | Added libcommon Maven repo |
| `app.json` | App name "Lumenai" → "Lumen AI" |
| `constants/strings.ts` | app.name, versionFooter, loginFooter → "Lumen AI" |
| `android/app/src/main/res/values/strings.xml` | app_name → "Lumen AI" |

## Files Outside Vestigia (UVCCamera project)

| File | What changed |
|------|-------------|
| `UVCCamera/build.gradle` | AGP 3.1.4→7.4.2, jcenter→mavenCentral, SDK 27→33 |
| `UVCCamera/gradle/wrapper/gradle-wrapper.properties` | Gradle 8.5→7.6.3 |
| `UVCCamera/gradle.properties` | Removed -XX:MaxPermSize=512m |
| `UVCCamera/libuvccamera/src/main/jni/Application.mk` | ABIs updated, platform android-14→android-21 |
| `UVCCamera/libuvccamera/src/main/java/.../USBMonitor.java` | PendingIntent.FLAG_IMMUTABLE for Android 12+ |

## Pending
- Install release APK on physical device (once build completes)
- Test UVC path with PureThermal Mini Pro connected via USB OTG
- End-to-end: bilateral FLIR capture → DPN API → classification result → save to Supabase

## Known Issues at End of Session
- UVC camera untested on physical device — emulator has no USB host stack
- Edge Function `auth-redirect` still not deployed
- `MOCK_ANGIOSOMES` in clinical-data.tsx (CODE-09) still deferred

## Next Session
1. Verify release APK installed and running on device
2. Connect PureThermal Mini Pro via USB OTG — confirm UVC frames appear in live-feed
3. Full bilateral capture → DPN API → result → save to cloud (end-to-end test)
4. Offline history screen + patient live-feed + patient history (plan file: `fluttering-scribbling-floyd.md`)
